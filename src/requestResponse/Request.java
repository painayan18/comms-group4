package requestResponse;

public class Request {
	//note from JP: I believe that the class diagram may be insufficient
		// we may need a string or something for ChatRoomData requests, 
		// with info from a ChatRoomDescription to specify which ChatRoom.
}
